const FilterType = {
  EVERYTHING: 'everything',
  FUTURE: 'future',
  PRESENT: 'present',
  PAST: 'past',
};

const EVENT_COUNT = 25;

const SortType = {
  DATE: 'date',
  TIME: 'time',
  PRICE: 'price',
};

export { FilterType, EVENT_COUNT, SortType};
